# inspired by the vwf planarDrag.vwf behaviour
---
methods:
  toggle:
scripts:
- |
  this.initialize = function() {
    this.future(0).toggle();
  };

  this.toggle = function() {
    this.visible = !this.visible;
  }; //@ sourceURL=togglevis.ux
